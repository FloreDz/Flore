<resources>
    <!-- Activating overflow menu offset -->
    <style name="OverflowMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <!-- Required for pre-Lollipop. -->
        <item name="overlapAnchor">false</item>

        <!-- Required for Lollipop. -->
        <item name="android:overlapAnchor">false</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionOverflowMenuStyle">@style/OverflowMenu</item>
        <!-- this color should be the most widely used across all screens and components. -->
        <item name="colorPrimary">@color/logo_blue_light</item>
        <!-- Palettes with a secondary color may use this color to indicate a related action or information.-->
        <item name="colorPrimaryDark">@color/logo_blue_medium</item>
        <!---The accent should be used for the floating action button and interactive elements-->
        <item name="colorAccent">@color/logo_green_medium</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>


    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="flotingButtonLabel" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/white_color</item>
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textSize">15sp</item>
        <item name="android:padding">5dp</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="CollapsingToolbarExpandedTextAppearance" parent="AppTheme.NoActionBar">
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0sp</item>
    </style>

    <style name="CollapsingToolbarCollapsedTextAppearance" parent="AppTheme.NoActionBar">
        <item name="android:textColor">@color/white_color_smoke</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
